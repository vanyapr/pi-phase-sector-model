# pifs proportional sector module package

Generated module package for `pi-fs`.

Files:

```text
pifs/proportion.py
docs/proportional-sector-arithmetic.md
tests/test_proportion.py
PULL_REQUEST.md
```

Suggested branch:

```text
feat/proportional-sector-arithmetic
```

Suggested commit:

```text
feat(locate): add exact proportional sector arithmetic
```

Run tests:

```bash
python -m unittest discover -s tests
```
